# Azure live core evidence — 2026-08-30

All commands below ran from the private Azure jump VM. Values that identify
the subscription, tenant, webhook, or credentials are intentionally omitted.

## Terraform apply

The production root used the private jump VM as the Terraform execution host,
with workload add-ons enabled, alerting disabled, and private endpoints and
flow logs disabled pending their external-input gates.

```text
Apply complete! Resources: 4 added, 0 changed, 0 destroyed.
```

The four resources were the Kyverno Helm release, the dependent rendered
ClusterPolicy Helm release, the Falco namespace, and the Falco Helm release.

## Parity convergence

The parity changes were then applied from the same private host. The staged
apply added the pinned Argo CD foundation and updated Falco with the Azure
custom rule, with no destruction. The final maintenance-enabled plan wrote the
AKS auto-upgrade and node-OS maintenance schedules, also with no destruction:

```text
Staged parity apply: Apply complete! Resources: 1 added, 1 changed, 0 destroyed.
Maintenance convergence: Apply complete! Resources: 0 added, 1 changed, 0 destroyed.
Final Terraform plan: No changes. Your infrastructure matches the configuration.
```

## Cluster readiness

```text
kubectl get nodes
aks-system-...   Ready   <none>   v1.35.7

kubectl get clusterpolicy block-unsigned-images-azure \
  -o jsonpath='action={.spec.validationFailureAction} rules={.spec.rules[*].name}'
action=Enforce rules=verify-image-signature verify-sbom-attestation verify-provenance-attestation
```

Kyverno admission pods were `Running` with the Azure Workload Identity pod
label, and the admission ServiceAccount had the rendered client-id
annotation. The private AKS API resolved and was reachable from the jump VM.

Falco live resources were in `falco-system`:

```text
daemonset.apps/falco                         1     1     1     1     1
deployment.apps/falco-k8s-metacollector      1/1   1     1
```

No Kubernetes pods were `Pending` in the final readiness check.

Argo CD live resources were in `argocd`; all five required pods were `1/1
Running`, and the server exposed only ClusterIP services on ports 80/443. No
ingress was configured. The Falco Helm values contained
`Shell Spawned In Signed Workload Pod` under `customRules`.

The AKS maintenance API listed both `aksManagedAutoUpgradeSchedule` and
`aksManagedNodeOSUpgradeSchedule`, each starting at `03:00` with offset
`+00:00`. The VM managed identity listed the bootstrap Blob container through
Azure AD authentication, confirming the private state/code path is usable from
the execution host.

## Admission rejection proof

A server-side dry-run Pod using the mutable image
`scs4c4dfbde.azurecr.io/supply-chain-demo:latest` was denied before creation.
The response identified all three enforced rules:

```text
admission webhook ... denied the request
block-unsigned-images-azure:
  verify-image-signature: failed to verify image ...:latest
  verify-provenance-attestation: image attestations verification failed
  verify-sbom-attestation: image attestations verification failed
```

This test created no workload and no registry artifact.

## Remaining closure blockers

- The Azure workflow candidates are present locally but are not on trusted
  `refs/heads/main`; therefore no trusted Azure image digest exists yet.
- The demo workload remains gated until that verified digest is released and
  locked.
- VNet flow logs remain disabled until the owner supplies a dedicated globally
  unique storage-account name and the required network-watcher permissions are
  confirmed.
- Discord alerting remains disabled because no webhook was supplied.
- A private GitHub runner is still required before disabling public ACR access
  and accepting private registry closure. The current workflow intentionally
  emits a read-only reviewed release artifact; it does not mutate the
  repository or deploy directly.

# Generated only by the trusted main-branch promotion job after signature,
# SPDX SBOM, SLSA provenance, and digest-lock verification.
workload:
  enabled: true

image:
  repository: "${ACR_LOGIN_SERVER}/${ACR_APPLICATION_REPOSITORY}"
  digest: "${IMAGE_DIGEST}"
